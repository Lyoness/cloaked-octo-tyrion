language: erlang
sudo: required
dist: trusty
group: dev
install: echo nope
script:
- ls -l /opt/jdk_switcher/
- ls -l /etc/profile.d/
- echo $JAVA_HOME
- type jdk_switcher | head -1
- jdk_switcher home openjdk8
