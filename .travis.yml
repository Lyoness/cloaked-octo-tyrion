language: __sugilite__

sudo: required

dist: trusty

env:
  global: STACK=sugilite

install: echo nope

script:
- find /etc/apt -name '*.list' | while read -r f ; do
    echo "---> $f:" ; cat "${f}" ;
  done
- docker run -v "${PWD}:/app" ubuntu:14.04 /app/munge-apt-sources
